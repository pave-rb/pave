<div class="space-y-4">
  <% page_actions = capture do %>
    <%= link_to t("backoffice.spaces.index.new_button"), new_backoffice_space_path, class: button_classes(:primary) %>
  <% end %>
  <%= render "shared/page_header", title: t("backoffice.spaces.index.title"), actions: page_actions %>

  <% if @spaces.any? %>
    <div class="card-glass overflow-hidden p-0">
      <table class="min-w-full divide-y divide-slate-200 text-sm">
        <thead class="bg-slate-50">
          <tr>
            <th scope="col" class="px-4 py-2 text-left text-xs font-semibold uppercase tracking-wide text-slate-500">
              <%= t("backoffice.spaces.index.table.name") %>
            </th>
            <th scope="col" class="px-4 py-2 text-left text-xs font-semibold uppercase tracking-wide text-slate-500">
              <%= t("backoffice.spaces.index.table.customers") %>
            </th>
            <th scope="col" class="px-4 py-2 text-left text-xs font-semibold uppercase tracking-wide text-slate-500">
              <%= t("backoffice.spaces.index.table.team_size") %>
            </th>
            <th scope="col" class="px-4 py-2 text-right text-xs font-semibold uppercase tracking-wide text-slate-500">
              <%= t("backoffice.spaces.index.table.actions") %>
            </th>
          </tr>
        </thead>
        <tbody class="divide-y divide-slate-200">
          <% @spaces.each do |space| %>
            <tr class="hover:bg-slate-50">
              <td class="px-4 py-2 align-top text-slate-900"><%= space.name %></td>
              <td class="px-4 py-2 align-top text-slate-700"><%= space.customers.size %></td>
              <td class="px-4 py-2 align-top text-slate-700"><%= space.users.size %></td>
              <td class="px-4 py-2 align-top">
                <div class="flex flex-wrap justify-end gap-1">
                  <%= link_to t("backoffice.spaces.index.view"), backoffice_space_path(space), class: button_classes(:table_link) %>
                  <%= link_to t("backoffice.spaces.index.edit"), edit_backoffice_space_path(space), class: button_classes(:table_link) %>
                  <%= link_to t("backoffice.spaces.index.manage_users"), backoffice_users_path(space_id: space.id), class: button_classes(:table_link) %>
                  <%= button_to t("backoffice.spaces.index.delete"), backoffice_space_path(space), method: :delete, data: { turbo_confirm: t("backoffice.spaces.index.delete_confirm"), turbo_submits_with: t("shared.deleting") }, class: button_classes(:danger) %>
                </div>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <%= paginate @spaces %>
  <% else %>
    <%= render "shared/empty_state", content: t("backoffice.spaces.index.empty") %>
  <% end %>
</div>
