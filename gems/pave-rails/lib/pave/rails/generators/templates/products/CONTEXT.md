# <%= class_name %> — Pavê Product

Product scaffold generated by `bin/pave new product <%= file_name %>`.

## Purpose

This product implements domain-specific business logic for the host app.
It uses the Pavê runtime for tenancy, identity, audit, billing, and backoffice.

## Directory Structure

```
products/<%= file_name %>/
  config/
    product.rb      -- Product registration
    backoffice.rb   -- Backoffice panel registration
    routes.rb       -- Product routes
  app/
    controllers/    -- Product controllers
    models/         -- Product models
    services/       -- Product service objects
    views/          -- Product views
  db/
    migrate/        -- Product migrations
  product.yml       -- Product manifest
  CONTEXT.md        -- This file
```
