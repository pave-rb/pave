# frozen_string_literal: true

# Pavê initializer — runs after the runtime is loaded.
#
# Use this file to configure Pavê runtime modules, set defaults,
# register backoffice panels, and wire up application-specific behavior.

# Example: Configure the backoffice navigation
# Pave::Backoffice.configure do |config|
#   config.site_name = "My Platform"
#   config.root_path = :root_path
# end
